[build]
  base = "solidworks-tracker"
  command = "npm run build"
  publish = "dist"
  # Netlify Functions devre dışı: bilinçli olarak geçersiz bir klasör veriyoruz
  functions = "disabled_functions"

[build.environment]
  NODE_VERSION = "20"